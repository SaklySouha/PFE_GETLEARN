{% extends 'base.html.twig' %}

{% block title %}Chapitre{% endblock %}

{% block body %}
    <h1>Chapitre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chapitre.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ chapitre.titre }}</td>
            </tr>
              <tr>
                <th>Cours</th>
                <td>{{ chapitre.cours.titre }}</td>
            </tr>
        </tbody>
    </table>

    <table class="borderless  mt-5" >
        <tr>
            <td  >  <button style="margin:6px;"  class="btn btn-primary"><a class="text-light btn "   href="{{ path('chapitre_index') }}">Back to list</a></button>
            </td>
            <td > <button style="margin:6px;"  class="btn btn-warning"><a class="text-light btn"   href="{{ path('chapitre_edit', {'id': chapitre.id}) }}">Edit</a></button></td>
            <td  >       {{ include('chapitre/_delete_form.html.twig') }}

            </td>
        </tr>
    </table>


{% endblock %}
